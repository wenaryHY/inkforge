# InkForge 阶段任务清单

> 用途：把 `executable-roadmap.md` 变成可执行、可勾选、可验收的阶段清单。
> 规则：只有通过构建与验收的事项才允许被勾选为完成。

---

## Phase 1 — 内容底座升级（已完成）

### 已确认完成
- [x] `posts.content_type` 接入
- [x] `page_render_mode` 接入
- [x] Tiptap 编辑器替换完成
- [x] 后台按内容类型创建 / 编辑 / 筛选
- [x] `/api/v1/...` 正式路由可用
- [x] 旧 `/api/...` 兼容层保留

---

## Phase 2 — 安装向导（Web 先行，已完成）

### 已确认完成
- [x] 安装状态枚举与回填逻辑
- [x] setup 请求 / 响应 DTO
- [x] setup service / repository / handler
- [x] `/setup` Web 安装向导页面
- [x] 安装完成后的运行态切换
- [x] 重复安装保护
- [x] setup 逻辑可被其他端复用

---

## Phase 3A — 基线加固与验收补齐（当前阶段）

### 构建与工具链
- [ ] 修复 `src-tauri/Cargo.toml` manifest 错误
- [ ] 补齐 `src-tauri` 缺失依赖并恢复 workspace 构建
- [ ] 统一 Tauri CLI 与 Node 依赖使用方式
- [ ] 让 `cargo check --workspace` 通过
- [x] 让 `npm run build --prefix src/admin/ui` 通过

### 已完成的安全与基线收敛
- [x] 默认 JWT secret 从“仅警告”升级为“生产阻断或显式不安全模式”
- [x] 主题 ZIP 上传增加路径校验，避免非法解压路径写入
- [x] 管理端 token 从 `localStorage` 迁移到更安全的 Cookie Session 方案
- [x] 登录链路增加基础限流 / 防爆破保护
- [x] 增加通用安全响应头中间件
- [x] Markdown 渲染输出已接入服务端 sanitize

### 当前待完成的主链路问题
- [x] 收口 setup 完成前公开注册仍可首建管理员的问题
- [x] 修复后台多页面根路径跳转导致跳出 `/admin` 的问题
- [x] 修复设置页可编辑字段与后端可写字段不一致的问题
- [x] 修复用户资料更新的伪 PATCH 覆盖问题
- [ ] 收口 `custom_html`、主题模板 `safe` 输出与通用 CSP 的内容安全边界
- [x] 修复搜索 `tag_id` 未生效与 `pagination.total` 失真的问题
- [ ] 规划 legacy `/api` 兼容层退出路径并统一弃用头策略
- [x] 打通 `X-Client-Request-Id`、日志链路与响应 `request_id`
- [ ] 统一备份、恢复、Docker、Litestream 的路径坐标
- [ ] 修正 SQLite 绝对路径 URL 的解析语义
- [ ] 统一后台静态入口 `index.html / admin.html` 的部署语义
- [ ] 修复 Tauri sidecar 对 setup 状态字段的读取不匹配

### 验收与治理
- [x] 更新 `INTEGRATION_TEST_CHECKLIST.md` 并按当前主链路重排
- [x] 确认 `PROJECT_STATUS.md` 与代码状态一致
- [x] 确认 `v0.3-progress.md` 只记录已验证完成事项
- [x] 收敛 `memories/README.md` 为纯导航入口
- [x] 修正“Markdown sanitize 未落地”的历史漂移描述
- [ ] 为 `Phase 3B` 明确启动模型与窗口模型的验收条件
- [ ] 为 Web 主链路建立最小 CI 验收入口

### 退出条件
- [ ] 构建链路恢复正常
- [ ] Web 主链路回归通过
- [ ] 高优先级边界问题完成
- [ ] 文档与实际状态重新一致

---

## Phase 3B — Tauri 运行壳落地

### 接口先行
- [ ] 固化桌面启动模型（仅在验收通过后定版）
- [ ] 固化窗口模型（默认单窗口优先，特例再论证）
- [ ] 固化 sidecar 资源、打包与目标三元组策略
- [ ] 固化进程生命周期与异常退出策略

### 实现
- [ ] 打通 `tauri dev`
- [ ] 打通桌面打包链路
- [ ] 未安装状态进入 `/setup`
- [ ] 已安装状态进入 `/admin`
- [ ] 退出时正确清理 sidecar 进程

### 验收
- [ ] 开发模式可稳定启动
- [ ] 打包产物可运行
- [ ] setup → admin 桌面闭环成立
- [ ] 无孤儿进程
- [ ] Web 与桌面共用同一套 setup 逻辑

---

## Phase 4 — 插件运行时基础版

### 接口先行
- [ ] 定义插件目录规范
- [ ] 定义 manifest 结构
- [ ] 定义生命周期与隔离边界
- [ ] 定义 Slot / Route 注册契约
- [ ] 定义最小权限声明模型

### 实现
- [ ] 完成插件扫描
- [ ] 完成插件启停
- [ ] 完成基础生命周期管理
- [ ] 完成基础 Slot 注入
- [ ] 完成基础 Route 注册
- [ ] 完成 demo 插件

### 验收
- [ ] demo 插件可安装、启用、停用
- [ ] 插件可注册一个页面或一个 Slot
- [ ] 插件崩溃不影响主进程
- [ ] 权限声明可被用户看见并被流程消费

---

## Phase 5 — 主题构建与发布基础版

### 接口先行
- [ ] 定义主题输入 / 输出目录规范
- [ ] 定义 `ink-data` 数据结构
- [ ] 定义构建命令输入输出
- [ ] 定义首批发布适配器协议
- [ ] 定义 MiniJinja SSR 与静态构建共存边界

### 实现
- [ ] 实现主题构建命令入口
- [ ] 实现 `ink-data` 注入
- [ ] 完成示例主题构建
- [ ] 完成至少一个发布目标
- [ ] 完成与现有主题系统的共存策略

### 验收
- [ ] 示例主题可构建
- [ ] 构建产物可读取 `ink-data`
- [ ] 发布目标可输出可部署资源
- [ ] 不影响现有 MiniJinja 运行链路

---

## Phase 6 — 生态治理与权限模型预留

### 接口先行
- [ ] 定义 Registry 索引格式
- [ ] 定义插件 / 主题包元数据规范
- [ ] 定义风险标签结构
- [ ] 定义权限声明展示链路
- [ ] 定义高级权限模型预留边界

### 实现
- [ ] 建立静态 Registry
- [ ] 接入风险标签展示
- [ ] 接入权限声明展示链路
- [ ] 为高级权限模型留出接口层

### 验收
- [ ] 插件 / 主题可被索引和发现
- [ ] 风险标签可被展示
- [ ] 权限声明可被安装流程消费
- [ ] 未来升级不会被当前设计阻塞

---

## 通用阶段关卡

### 开始前
- [ ] 已完成需求澄清
- [ ] 已明确说出“已无任何假设”
- [ ] 已定义接口 / 类型 / 迁移 / 契约

### 完成前
- [ ] 已完成主链路演示或回归
- [ ] 已完成构建验证
- [ ] 已完成阶段验收
- [ ] 已更新 `PROJECT_STATUS.md`
- [ ] 已更新 `v0.3-progress.md`
