# InkForge v0.3 进度记录

> 规则：本文件只记录两类信息：
> 1. **已验证完成** 的工作；
> 2. **进行中 / 验收失败** 但会影响后续路径判断的重要尝试。
>
> 未通过构建、测试或主链路验收的工作，不得记为“完成”。

---

## 已验证完成

### 2026-04-09 — Phase 1 内容底座升级
- ✅ `content_type` 字段接入（`post | page`）
- ✅ `page_render_mode` 字段接入（`editor | custom_html`）
- ✅ Tiptap 替换 Milkdown，支持可视化 / 源码双模式
- ✅ 媒体插入链路在新编辑器下保持可用
- ✅ `/api/v1/...` 正式路由层与旧 `/api/...` 兼容层并存
- ✅ 前端主要 API 调用迁移到 `/api/v1/...`
- ✅ `Posts` 页面支持文章 / 页面创建与筛选

### 2026-04-09 — 内容底座后续修复
- ✅ 页面查看逻辑按渲染模式区分路径
- ✅ 自定义 HTML 上传后回写 `custom_html_path`
- ✅ Markdown 与自定义 HTML 双内容保留，不再互斥清空
- ✅ 页面渲染优先级与编辑器提示修正

### 2026-04-09 — 国际化与治理文档
- ✅ 管理后台 i18n 体系与中英切换落地
- ✅ 新增执行路线图、阶段清单、运行期规则
- ✅ 更新会话强制读取清单

### 2026-04-10 — Phase 2 安装向导（Web 先行）
- ✅ 新增 `setup` 模块（status / initialize DTO、handler、service、repository）
- ✅ 新增 `setup_completed`、`setup_stage` 与 `admin_url` 设置项
- ✅ 安装状态升级为四阶段模型：`not_started / admin_created / configured / completed`
- ✅ 旧库兼容回填：已有用户但缺失安装态字段时自动回填
- ✅ 未安装时 `/`、`/admin` 自动进入 `/setup`
- ✅ `/setup` 首装向导：站点信息、管理员、后台入口、公开注册开关
- ✅ 安装完成后自动切入正常后台流程
- ✅ `site_url` / `admin_url` 统一校验并刷新运行时缓存

### 2026-04-10 — Phase 3A setup / register 边界收口
- ✅ `auth::register()` 已复用 setup 完成态守卫，并尊重 `allow_register` 开关
- ✅ 首个管理员只能由安装向导创建，公开注册用户固定为 `member`
- ✅ 登录页会先读取 `/setup/status`，未安装时跳转 `/setup`
- ✅ 登录页仅在 `installed && allow_register=true` 时展示注册入口
- ✅ 已通过 `npm run build --prefix src/admin/ui` 与 `cargo check -p inkforge` 验证

### 2026-04-10 — Phase 3A 后台路由与设置契约收口
- ✅ 管理后台遗留的根路径跳转已统一收敛到 `/admin/*`
- ✅ `Settings.tsx` 已接通 `trash_retention_days`、`trash_cleanup_hour`、`trash_cleanup_minute` 的真实保存链路
- ✅ 设置服务已为回收站保留天数与清理时间增加后端白名单与范围校验
- ✅ `me/profile` 已改为真正 PATCH 语义，未提交字段会保留当前值
- ✅ 默认主题与后台语言切换已改为只提交实际变更字段
- ✅ 已通过 `npm run build --prefix src/admin/ui` 与 `cargo check -p inkforge` 验证

### 2026-04-10 — 文档治理与架构审计整理
- ✅ 收敛 `memories/README.md` 为纯导航索引，不再承载状态结论
- ✅ 校正 `PROJECT_STATUS.md`，恢复“唯一真相源”角色
- ✅ 更新 `architecture-decisions.md` 与 `phase-task-checklist.md`，移除失真的基线描述
- ✅ 删除已被决策与路线图覆盖的 `v0.3-architecture-vision.md`，避免旧愿景继续干扰后续记忆
- ✅ 生成基于真实代码的 `architecture-audit-2026-04-10.md`
- ✅ 将品牌资产拆分为 `logo.svg`（完整锁定版）、`logo-icon.svg`（图标）与 `logo-wordmark.svg`（字标）

### 2026-04-10 — Phase 3A 搜索契约与请求追踪收口
- ✅ 公开搜索已修复 `tag_id` 过滤与 `pagination.total` 真实总数语义
- ✅ 搜索查询与计数已统一过滤条件，避免标签筛选下重复行与 total 失真
- ✅ 全局 request_id 中间件已接入，响应体与响应头统一复用同一追踪 ID
- ✅ 管理后台与默认主题在异常场景下都可读取并记录服务端 `request_id`
- ✅ 已通过 `npm run build --prefix src/admin/ui` 与 `cargo check -p inkforge` 验证

### 2026-04-10 — 第二轮审计补证与文档同步
- ✅ 确认真实迁移入口已收敛到 `src/main.rs` 的 `sqlx::migrate!("./migrations")`
- ✅ 确认 `src/db.rs` 与 `src/infra/db/pool.rs` 为旧迁移残留，不代表真实运行链路
- ✅ 确认默认主题仍真实依赖 legacy `/api/...`，兼容层暂不能视为空壳
- ✅ 确认 Markdown 渲染输出已接入服务端 sanitize，并同步修正文档中的过时表述
- ✅ 扩写 `architecture-audit-2026-04-10.md`，补齐迁移、兼容层、搜索、可观测性、CI 与部署矩阵审计
- ✅ 同步 `PROJECT_STATUS.md`、`phase-task-checklist.md`、`INTEGRATION_TEST_CHECKLIST.md` 与根 `README.md` 到当前代码事实

---

## 进行中 / 待重新验收

### 2026-04-10 — Phase 3 Tauri 骨架尝试（未通过验收）
本次尝试已经引入以下内容：
- 已新增 `src-tauri/` 工程骨架
- 已新增 workspace 成员与前端 Tauri IPC 接线尝试
- 已新增 sidecar、窗口管理、setup → admin 跳转相关代码

但截至当前审计，**不能记为完成**，原因包括：
- `cargo check --workspace` 失败
- `src-tauri/Cargo.toml` 存在 manifest 错误
- `src-tauri` 依赖未完整声明
- 前端对 `@tauri-apps/api` 的构建闭环未完成
- Tauri 资源与 sidecar 打包链路未闭环
- sidecar 对 setup 状态字段的读取仍存在契约不匹配风险

**结论**：`Phase 3` 当前状态应为“进行中 / 待验收”，而不是“已完成”。

**最后更新**: 2026-04-10
