# InkForge v0.3 开发进度

## 已完成

### 阶段1: 备份系统 ✅
- 本地备份（数据库 + 媒体文件 zip 打包）
- 备份恢复（上传 zip / 从已有备份 ID 恢复）
- 定时备份调度（daily / weekly / monthly）
- 备份列表、下载、删除 API
- S3 备份代码占位（运行时未接线）

### 阶段2: SEO 优化 ✅（部分）
- Sitemap 生成（`/sitemap.xml`）
- Robots.txt（`/robots.txt`）
- Meta 标签构建器已编写（`seo/meta.rs`），待前台渲染时接入

### 阶段3: WebSocket 实时通知 ✅
- `/ws/admin` 管理员事件通道
- `/ws/public` 公开事件通道

### 阶段4: 主题系统 ✅
- 主题扫描（读 themes/ 目录下 theme.toml）
- 主题列表 / 详情 / 激活 / 上传 API
- 主题配置保存（schema → 数据库 theme_configs 表）
- 默认主题（3 个模板：index / post / profile）

### 阶段5: Media 分类增强 ✅
- media_categories 表（6 个默认分类）
- 分类 CRUD API
- 前端 MediaCategorySelect / MediaCategoryBadge 组件
- Upload 页面集成分类选择与过滤

### 阶段6: 前端管理后台 ✅
- 10 个管理页面构建完成
- React + TypeScript + Vite
- 16 个通用 UI 组件

## 未完成 / 待实现

- ✅ 前台主题渲染路由（router.rs 中 TODO）
- ✅ 国际化系统（i18n）- 中英文切换
- ⬜ 安装向导
- ⬜ 插件系统（仅有 trait 占位）
- ⬜ Tauri 桌面应用集成
- ⬜ JavaScript SDK

## 新增完成（2026-04-09）

### Phase 1 内容底座升级
- ✅ `content_type` 字段（post | page）+ migration 009
- ✅ `custom_html_path` 字段 + 自定义 HTML/ZIP 页面上传
- ✅ Tiptap 编辑器替换 Milkdown（源码/可视化双模式 + 媒体库联动）
- ✅ API v1 路由层 + legacy 兼容 + Deprecation header
- ✅ 前端 Posts.tsx 文章/页面 Tab 切换
- ✅ 前端所有 API 路径迁移到 `/api/v1/`

### Phase 1 Bug 修复与冲突解决（2026-04-09 晚）
- ✅ 新增 `page_render_mode` 字段（editor | custom_html），migration 010
- ✅ 修复上传自定义 HTML 后未 PATCH 回 `custom_html_path` 的 bug
- ✅ 双内容保留：MD 和自定义 HTML 不再互斥清空，两边都保存
- ✅ 保存时弹窗选择：页面同时有 MD 和 HTML 内容时，弹出渲染模式选择弹窗
- ✅ 页面查看按钮：自定义 HTML 页面跳 `/pages/{slug}`，MD 页面跳 `/posts/{slug}`
- ✅ 前台渲染优先级：`page_render_mode = "custom_html"` → 直接 serve HTML；`"editor"` → 主题模板渲染 MD
- ✅ 编辑器模式切换提示：显示当前前台使用的渲染模式

### Bug 修复
- 修复登录后页面不跳转的问题
- 修复 5173 端口管理页白屏（BrowserRouter basename 配置）

### 国际化系统（i18n）
- 创建 i18n 核心架构（Context + Hook + 字典系统）
- 按功能拆分的字典文件（common, nav, login, posts, settings, content）
- 语言检测与持久化（localStorage + 后端同步）
- 语言切换按钮（Login 页面、Sidebar、Settings 页面）
- 数据库 migration 添加 users.language 字段
- 后端 API 支持语言设置更新
- 所有管理页面完成国际化：
  - Categories.tsx（分类管理）
  - Tags.tsx（标签管理）
  - CommentsV2.tsx（评论管理）
  - Themes.tsx / ThemeDetail.tsx（主题管理）
  - RecycleBin.tsx（回收站）
  - Posts.tsx（文章管理）
  - Upload.tsx（媒体管理）
  - MediaCategories.tsx（媒体分类管理）

### 规划与治理文档
- 新增 `memories/executable-roadmap.md`，将架构愿景改写为可执行阶段路线图
- 新增 `memories/phase-task-checklist.md`，为每个 Phase 提供可勾选任务清单
- 新增 `memories/RUNTIME_RULES.md`，固化每次会话的提问、风险分级与执行协议
- 更新 `CODEBUDDY.md` 强制读取清单，确保每次会话都会读取运行期规则
- 更新 `memories/PROJECT_STATUS.md`，同步新的执行路线图与治理文档入口

## 新增完成（2026-04-10）

### Phase 2 安装向导 MVP
- ✅ 新增 `setup` 模块（status / initialize DTO、handler、service、repository）
- ✅ 新增 `setup_completed`、`setup_stage` 与 `admin_url` 设置项（migration 012 + 013）
- ✅ 安装状态升级为 `not_started / admin_created / configured / completed` 四阶段模型
- ✅ 旧库兼容回填：已有用户但缺失新安装态字段时，启动期自动回填到显式阶段模型
- ✅ 未安装时 `/`、`/admin` 自动引导到 `/setup`
- ✅ `/setup` 首装向导：站点信息、`site_url`、`admin_url`、公开注册开关、首个管理员创建
- ✅ 安装完成页 + 自动跳转后台入口
- ✅ `site_url` / `admin_url` 统一 URL 校验与运行时缓存刷新
- ✅ 后台设置页新增 `admin_url`；前台主题后台入口与后台“查看文章”改为基于配置生成

**最后更新**: 2026-04-10
